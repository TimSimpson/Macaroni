~unit DogDriver
    type=exe
;

~import std::cout;

class GoDog
{
    ~block "h-predef"
    {
        #include <Dog.h>
    }

     public ~global void main(const char * args, int argc)
     {
        std::cout << "HELLO DOGGY.";
        Dog dog;
        std::cout << "Dogs have " << dog.GetLegs() << " leg(s).";
     }
};
