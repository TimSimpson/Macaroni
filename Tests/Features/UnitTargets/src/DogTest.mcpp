~unit DogTest
    ~type=test
;

//TODO:
~import std::cout;
~import Dog;

~block "cpp"
{
    void main(const char * * args, int argc)
    {
        std::cout << "HELLO DOGGY.";
        Dog dog;
        std::cout << "Dogs have " << dog.GetLegs() << " leg(s).";
    }
}
