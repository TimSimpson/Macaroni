#ifndef MACARONI_COMPILE_GUARD_Macaroni___H
#define MACARONI_COMPILE_GUARD_Macaroni___H
/* ~ Debug Output of Dependency Graph ~ */
/* Macaroni::Model
     ld    : // ~ <(I don't know how to generate definition for Macaroni::Model.)
 
     hd    : #include <Macaroni/_.h>
 
     using : // ~ <(I don't know how to generate a using statement for Macaroni::Model.) 
 
     light dependencies : 

     HEAVY dependencies : 
*/
/* Macaroni::Build
     ld    : // ~ <(I don't know how to generate definition for Macaroni::Build.)
 
     hd    : #include <Macaroni/_.h>
 
     using : // ~ <(I don't know how to generate a using statement for Macaroni::Build.) 
 
     light dependencies : 

     HEAVY dependencies : 
*/
/* Macaroni::CmdLine
     ld    : // ~ <(I don't know how to generate definition for Macaroni::CmdLine.)
 
     hd    : #include <Macaroni/_.h>
 
     using : // ~ <(I don't know how to generate a using statement for Macaroni::CmdLine.) 
 
     light dependencies : 

     HEAVY dependencies : 
*/


namespace Macaroni { 

//     ~ Have no way to handle node Model with Member type Namespace.
//     ~ Have no way to handle node Build with Member type Namespace.
//     ~ Have no way to handle node CmdLine with Member type Namespace.

} // End namespace 

#endif // end of MACARONI_COMPILE_GUARD_Macaroni___H